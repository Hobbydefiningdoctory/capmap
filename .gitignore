# Compiled output
dist/
*.tsbuildinfo

# Dependencies
node_modules/

# Generated files (run `node bin/capman.js generate` to recreate)
manifest.json

# Environment variables — never commit real keys
.env

# Replit internals
.cache/
.local/
.replit
.replitignore

# pnpm / monorepo internals
pnpm-lock.yaml
pnpm-workspace.yaml

# macOS
.DS_Store

# IDEs
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
